/* Operator key for the per-PC dashboards. Scope: change what ONE PC shows, plus
   save a composed banner. It cannot upload files, generate AI, delete anything, or
   touch another PC. Public by design so the PC dashboards need no login. */
window.ACBZ_OP = "4c6fcaed97f672da69b7186bdef35df9";
